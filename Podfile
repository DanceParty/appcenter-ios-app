# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'appcenter-testapp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for appcenter-testapp
  pod 'AppCenter'

  target 'appcenter-testappTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'appcenter-testappUITests' do
    # Pods for testing
  end

end
